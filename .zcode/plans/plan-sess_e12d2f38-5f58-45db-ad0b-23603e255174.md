## What's already done (verified)

- **Downloaders (8):** full tool configs in `tools.ts`, `ToolPageView`, `/api/download` dispatching to `ytdlp`/`gallerydl` service layers, proxy, caching, rate limiting. ✅ Live.
- **PDF backend:** `pdfService.ts` + 5 routes (merge, split, compress, jpg-to-pdf, pdf-to-jpg). ✅ Exists but **not wired to UI**.
- **Image backend:** `imageService.ts` + `/api/tools/image` (8 ops). ✅ Exists but **not wired to UI**.
- **QR backend:** `/api/tools/qr/generate` + `/api/tools/qr/scan`. ✅ Exists but **not wired to UI**.
- **Color Picker & Gradient Generator UI components** exist (no backend needed). ✅ Exist but **not wired to UI** — they currently render "Coming Soon".
- **Coming Soon page** (`GenericToolPage`) ✅.

## What's missing = the task to complete

Every non-downloader tool page currently falls through to the "Coming Soon" `GenericToolPage`, even the ones with finished backends/components. The job is to **wire up all supported tools** to live UIs and leave only genuinely-unsupported tools as Coming Soon.

## Implementation Report (auto-chosen per the rules)

| Tool | Method | Library/API | License | Commercial | Cost | Status |
|---|---|---|---|---|---|---|
| Image Compressor | OSS | Sharp (libvips) | Apache-2.0 | ✅ | Free | **Implement** |
| Image Resizer | OSS | Sharp | Apache-2.0 | ✅ | Free | **Implement** |
| Image Converter | OSS | Sharp | Apache-2.0 | ✅ | Free | **Implement** |
| Image Enhancer | OSS | Sharp | Apache-2.0 | ✅ | Free | **Implement** |
| JPG→PNG | OSS | Sharp | Apache-2.0 | ✅ | Free | **Implement** |
| PNG→JPG | OSS | Sharp | Apache-2.0 | ✅ | Free | **Implement** |
| WEBP Converter | OSS | Sharp | Apache-2.0 | ✅ | Free | **Implement** |
| HEIC→JPG | OSS | Sharp | Apache-2.0 | ✅ | Free | **Implement** |
| Merge PDF | OSS | pdf-lib | MIT | ✅ | Free | **Implement** |
| Split PDF | OSS | pdf-lib | MIT | ✅ | Free | **Implement** |
| Compress PDF | OSS | pdf-lib | MIT | ✅ | Free | **Implement** |
| JPG→PDF | OSS | pdf-lib + Sharp | MIT/Apache | ✅ | Free | **Implement** |
| PDF→JPG | OSS | pdf-lib + Sharp (poppler) | MIT/Apache | ✅ | Free | **Implement** |
| QR Code Generator | OSS | qrcode | MIT | ✅ | Free | **Implement** |
| QR Code Scanner | OSS | jsqr + Sharp | MIT/Apache | ✅ | Free | **Implement** |
| Color Picker | OSS | browser EyeDropper + canvas | — | ✅ | Free | **Implement** |
| Gradient Generator | OSS | pure CSS | — | ✅ | Free | **Implement** |
| Image Upscaler | OSS (AI) | Real-ESRGAN | BSD-3 | ✅ | Free | **Coming Soon** (heavy GPU model, not viable in Next serverless; needs separate GPU service) |
| Background Remover | OSS (AI) | U²-Net / RMBG | MIT/Apache | ✅ | Free | **Coming Soon** (same — heavy model, separate service) |
| Video Converter/Compressor/MP3/GIF | OSS | FFmpeg | LGPL/GPL | ✅ | Free | **Coming Soon** (FFmpeg not installed on this host; install + new service needed) |
| PDF→Word / Word→PDF | — | no free OSS route | — | — | — | **Coming Soon** (only paid OCR/SaaS APIs exist) |
| All AI generators (thumbnail/title/caption/etc.) | — | no free, commercially-usable, reliable API | — | — | — | **Coming Soon** (rules forbid paid APIs) |
| All SEO generators | — | no free API | — | — | — | **Coming Soon** |

## Plan

### 1. New `functionalTools` registry — `src/config/functionalTools.ts`
A map: `slug → { Component, acceptsFileMultiple?: boolean }`. Imports the existing `ColorPickerTool`, `GradientGeneratorTool`, and the new components below. Exported helper `getFunctionalTool(slug)`.

### 2. Shared layout — `src/components/tools/FunctionalToolLayout.tsx`
Reuses the existing hero chrome (breadcrumb, icon tile, gradient blobs, "Free tool, no signup" badge) but renders the passed-in interactive `<Component/>` where the `DownloaderForm` would go, plus a How-it-works / FAQ block driven by a small static content map so each page still reads as a complete tool page. Kept lightweight to avoid duplicating `ToolPage.tsx`.

### 3. New interactive tool components (all `'use client'`)
- `ImageTool.tsx` — single component, prop-driven `op` (compress/resize/convert/enhance/jpg-to-png/png-to-jpg/to-webp/heic-to-jpg). File dropzone, per-op controls (quality slider, width/height inputs, format select), POST to `/api/tools/image`, preview + download button + size-delta readout. Reused across all 8 image slugs.
- `PdfTool.tsx` — single component, prop-driven `op` (merge/split/compress/jpg-to-pdf/pdf-to-jpg). Multi-file dropzone where needed, ranges input for split, POST to the matching `/api/tools/pdf/*` route, result download(s). Reused across 5 PDF slugs.
- `QrGeneratorTool.tsx` — text input, format (png/svg/base64), size, colors, EC level; live preview; calls `/api/tools/qr/generate`.
- `QrScannerTool.tsx` — file dropzone, calls `/api/tools/qr/scan`, shows decoded text + detected type with copy button.

### 4. Wire routing — edit `src/app/tools/[slug]/page.tsx`
Add a third path: after the `toolsBySlug` check, call `getFunctionalTool(params.slug)`; if present render `<FunctionalToolLayout>` with the tool's catalog metadata + the component. Update `generateMetadata` so functional tools get a clean (non-"Coming Soon") title. (Static params already include all catalog slugs, so no `generateStaticParams` change needed.)

### 5. Fix 2 pre-existing TypeScript errors (so the build is clean)
- `src/lib/auth.ts:52` — `role` type narrowing.
- `src/app/api/analytics/route.ts:28` — `meta` Prisma typing.

### 6. Verification
- `npx tsc --noEmit` clean.
- `npm run build` clean.
- Spot-check that newly-live slugs render the functional UI and the remaining (upscaler, video, AI, SEO, pdf-to-word…) still render `GenericToolPage`.

No new backend code needed — all routes already exist. No new dependencies. No `.env` changes. Everything reuses existing UI primitives (Button, Input, Section, SectionHeading, Breadcrumb, Reveal, Modal, Accordion, Toast, CopyButton) and the design tokens already in `tailwind.config.ts`.